import { DistinctvaluePipe } from './distinctvalue.pipe';

describe('DistinctvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new DistinctvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
