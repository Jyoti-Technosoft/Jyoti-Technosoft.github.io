// ***
// *******
// Note : Please do pass your config direct to the envirment just put in configuration variables then pass it
// *******
// ***

let configWebsite = {
  showClientReview: false,
};

// Exporting envirment
export const environment = configWebsite;
